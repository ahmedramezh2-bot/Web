# scene-graph

The one continuous scene graph every chapter shares. No chapter creates its own scene — everything mounts into this graph so the camera can travel between chapters without a reload/unmount.
